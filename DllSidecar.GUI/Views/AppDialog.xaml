<Window x:Class="DllSidecar.GUI.Views.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="DllSidecar"
        SizeToContent="Height" Width="460"
        MinHeight="200" MaxHeight="540"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource Base}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32" ResizeBorderThickness="0"
                            GlassFrameThickness="0" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>       <!-- 0: title strip (Crust band) -->
            <RowDefinition Height="Auto"/>     <!-- 1: header (icon + title) -->
            <RowDefinition Height="*"/>        <!-- 2: body text -->
            <RowDefinition Height="Auto"/>     <!-- 3: actions -->
        </Grid.RowDefinitions>

        <!-- Custom title strip (matches MainWindow chrome) -->
        <Border Grid.Row="0" Background="{StaticResource Crust}">
            <DockPanel>
                <Button DockPanel.Dock="Right" x:Name="CloseStripBtn"
                        Style="{StaticResource HudWindowCloseButton}"
                        Click="Close_Click" ToolTip="Close (Esc)"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <TextBlock Text="&#x2715;" FontSize="14"/>
                </Button>
                <TextBlock x:Name="ChromeTitle" Text="DllSidecar"
                           VerticalAlignment="Center" Margin="14,0,0,0"
                           Foreground="{StaticResource Overlay}" FontSize="11"
                           FontFamily="{StaticResource FontMono}"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>

        <!-- Header -->
        <Grid Grid.Row="1" Margin="20,16,20,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="IconGlyph" FontSize="22"
                       VerticalAlignment="Center" Margin="0,0,12,0"
                       Foreground="{StaticResource Text}"/>
            <TextBlock Grid.Column="1" x:Name="TitleText" FontSize="14" FontWeight="SemiBold"
                       VerticalAlignment="Center" Foreground="{StaticResource Text}"
                       TextTrimming="CharacterEllipsis"/>
        </Grid>

        <!-- Body -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="20,4">
            <TextBlock x:Name="BodyText" TextWrapping="Wrap" FontSize="13"
                       Foreground="{StaticResource Subtext}" LineHeight="20"/>
        </ScrollViewer>

        <!-- Actions -->
        <StackPanel Grid.Row="3" x:Name="ButtonStrip" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="20,16,20,18"/>
    </Grid>
</Window>
