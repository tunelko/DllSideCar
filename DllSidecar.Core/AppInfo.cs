namespace DllSidecar.Core;

/// <summary>Single source of truth for the app version displayed in UI.</summary>
public static class AppInfo
{
    public const string Version = "1.1.15";
    public const string VersionDisplay = "v" + Version;
}
